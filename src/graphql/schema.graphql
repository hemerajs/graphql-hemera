type User {
  id: ID!
  email: String!
  name: String!
}
input UserInput {
  email: String
  name: String
}
type Query {
  getUserById(id: ID!): User
  getUserByEmail(email: String!): User
}
type Mutation {
  createUser(input: UserInput): User
}
schema {
  query: Query
  mutation: Mutation
}
